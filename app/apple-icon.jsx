import { buildFinanceIcon, contentType } from './icon';

export { contentType };

export const size = {
  width: 180,
  height: 180,
};

export default function AppleIcon() {
  return buildFinanceIcon(size);
}
